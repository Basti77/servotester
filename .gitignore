# ---- Android / Gradle (app/) ----
app/build/
app/.gradle/
app/local.properties
app/.idea/
app/*.iml
app/.kotlin/
build/
.gradle/
local.properties
*.iml
.idea/
.DS_Store

# Keep debug APK out of git — releases carry the built artifacts instead.
*.apk
*.aab

# ---- Arduino firmware (firmware/) ----
# Build output is regenerated by arduino-cli; releases carry the .bin instead.
firmware/build/

# ---- misc ----
*.log
